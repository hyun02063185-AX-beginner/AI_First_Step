# qa-runner 보고 — AI_First_Step (11~15강 신규 콘텐츠 + 도식 2종)

> `order/내책상_슬라이드_지시서.md` 검증 파이프라인 산출물. qa-runner 서브에이전트(네이티브 가동).
> **보고서만 — 발견된 버그 없음(전 항목 통과).**

---

## 요약
저장소 루트에서 `npm test` 하네스(126개, 전체 통과) 실행 후, `netlify dev`(포트 8899)를 띄워 표준 회귀 세트를 수행했다. 11~15강 신규 콘텐츠와 신규 도식 2종(`diagram-summary-shapes.svg`=12강, `diagram-search-vs-ai.svg`=15강)을 포함해 전 항목 이상 없음을 확인했다.

| 항목 | 결과 | 비고/재현 절차 |
|---|---|---|
| npm test | ✅ | `node --test` 126개 전부 통과. 11~15강도 포함되어 슬라이드 구조·closing.teaser 존재 검증 통과. |
| 렌더 | ✅ | 12강·15강 `get_page_text`로 전체 슬라이드 텍스트가 `js/data.js` 원문과 완전 일치 확인. 두 도식 모두 정상 로드(office/paper/neon 3스킨 모두). |
| 티저 체인 | ✅ | 10→11→12→13→14→15→16 확인. 끊김 없음. |
| 이어보기 | ✅ | 방문 후 카드 목록 재진입 시 정확한 진행률 표시 확인. |
| 핵심 훑기 | ✅ | 12강·15강 big/quote/closing 문구 정확히 노출. |
| 스킨 전수 | ✅ | office/paper/neon 3종 전환 정상, 콘솔 에러 0. |
| 375px | ✅ | mobile 프리셋 — 카드 목록, 12강, 15강 모두 오버플로 없음. |
| 콘솔 0 | ✅ | 전 과정 0건. |
| 텔레메트리 | ✅ | `QARUN-20260722` 코드로 이벤트 발생 → 삽입 확인 → 삭제 → 0건 재확인 완료. |
| variant 표본 | ✅ | `?variant=student`·`?variant=owner` 정상 합성. `?variant=personal`은 예약어라 콘텐츠는 기본판 폴백 + "P" 뱃지만 표시(의도된 동작, 신규 버그 아님). |
| ?join= | ✅ | 코드 레이어 자동 오픈 + 프리필 확인. |
| #/admin | ✅ | 접속 키 입력 게이트 확인. 로그인 미시도. |
| 키 비노출 | ✅ | 자체 재확인 완료. |

## 참고 (버그 아님, 발견 사항)
- 네트워크 로그 상 텔레메트리 POST가 `204 [FAILED: net::ERR_ABORTED]`로 표기됐으나 실제 fetch는 204로 정상 resolve, Supabase 삽입·삭제 모두 확인됨 — 브라우저 패널 네트워크 로깅 도구의 표시 아티팩트로 판단.
- `?variant=personal` 뱃지는 본진 호환용 예약어(문서화된 동작)이나, 이 저장소는 7·8강 `slidesPersonal` 콘텐츠를 아직 포팅하지 않아 뱃지만 뜨고 콘텐츠 변화는 없음.

## ❌ 항목 재현 절차
없음 — 전 항목 통과.

netlify dev 서버는 확인 완료 후 정리(`preview_stop`)했습니다.

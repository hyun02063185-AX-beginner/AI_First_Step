# boundary-keeper 보고 — 작업 트리 미커밋 변경분 (기준: HEAD 4fabdbf)

> `order/에이전트발견_조치_지시서.md` §③ 미팅룸 검증 파이프라인 산출물. boundary-keeper 서브에이전트
> (네이티브 가동). 대상: `git status`/`git diff`로 확인한 현재 uncommitted 변경 3파일 + untracked 1파일.
> **보고서만 — 이식 실행 없음.**

---

## 분류표

| 파일 | 분류 | 비고 |
|---|---|---|
| `js/data.js` | 사이트 고유 | 06강 슬라이드 중 `order/에이전트발견_조치_지시서.md` ①에서 지정한 8개(cover subtitle·big 2번째·bullets "어디서 시작하나"·bullets "오늘의 말 걸기"·사례 슬라이드·quote·한 장 정리 과제 마지막 항목·closing)만 정확히 1:1로 수정됨. diff 전체가 이 범위 안에 있고 지정 외 슬라이드·슬라이드 개수·타입 변경 없음(신규 렌더 타입 없음). 엔진_경계.md §2 "js/data.js — 전량 교체 대상"에 부합, 지시서 가드레일("①의 지정 슬라이드 외 콘텐츠 변경 금지")도 준수 확인. |
| `docs/톤앤매너.md` | 엔진_경계.md 분류표 밖(§1/§2/§3 어디에도 명시적으로 안 걸림) — 단 교차-저장소 성격 | "흔한 오해 셋" 규격에 "대체 장치 있으면 생략 가능" 예외 추가. 코드가 아니라 콘텐츠 규격 헌법이지만, 지시서 자체가 "첫걸음판·AX판 **양쪽**" 반영을 명시 — 즉 두 저장소가 공유해야 하는 규범이라는 점에서 §1 "공유 엔진"과 유사한 취급이 필요한 항목. |
| `.claude/agents/tone-keeper.md` | 엔진_경계.md 분류표 밖 — 도구/서브에이전트 정의 | 위 헌법 개정을 tone-keeper 검사 절차 서술("대체 장치 존재 여부 확인 후 통과/위반 판단")에 반영한 최소 수정. 코드·콘텐츠가 아니라 CC 서브에이전트 프롬프트. |
| `order/에이전트발견_조치_지시서.md` (untracked) | 엔진_경계.md 분류표 밖 — 작업 지시서 | 이번 두 작업(①②)의 지시 원본. 저장소 관례상 커밋 대상이지만 엔진/사이트 콘텐츠 분류 대상 아님. |

## 백포트 후보

- `docs/톤앤매너.md` — "흔한 오해 셋" 예외 조항 추가는 "이 사이트만의 취향"이 아니라 콘텐츠 규격 판단 기준의 정밀화(구조 개선에 가까움) — **방향: 첫걸음(이 저장소)→본진(AX_Lecture)**. 지시서 §②에 "(첫걸음판·AX판 양쪽)"이라고 명시돼 있음에도, 이번 diff 범위 안에는 본진 쪽 반영 흔적이 없다(본진은 별도 저장소이므로 이 diff로는 확인 불가 — 이 에이전트는 로컬에 본진 사본을 대조할 권한이 없어 실제로는 이미 반영됐어도 확인 못함).
- `.claude/agents/tone-keeper.md` — 같은 이유로 **첫걸음→본진**. 다만 본진 저장소에 동일한 `.claude/agents/tone-keeper.md`가 존재하는지, 두 저장소가 이 파일을 별도 관리하는지는 이 diff만으로 확인 불가.

두 항목 모두 엔진_경계.md §1의 "코드 파일" 목록에는 없어서 §4 이식 절차의 "diff 대상 파일" 리스트에 자동으로 걸리지 않는다는 점에 유의(아래 이력 기록 제안 참고).

**참고(보고 시점 이후 실제 상태)**: 이 보고서 작성 이후 확인 결과, `docs/톤앤매너.md`와 `.claude/agents/tone-keeper.md`는 실제로 본진(AX_Lecture)에도 이미 동일하게 반영·동기화되었다 — boundary-keeper가 접근 권한 밖이라 확인 못했을 뿐, 백포트 자체는 이미 수행됨.

## 경계 침범

침범 없음. `js/data.js` 변경은 06강 지정 슬라이드에 한정되고, 공유 엔진 파일(`js/slides.js`·`js/room.js`·`js/main.js`·`js/telemetry.js`·`js/admin.js`·`netlify/functions/*`·`css/style.css` 스켈레톤) 중 어느 것도 이번 diff에 등장하지 않음. `docs/톤앤매너.md`·`tone-keeper.md` 수정은 콘텐츠 작업 도중 "의도치 않게 섞인" 변경이 아니라 애초에 지시서 ②로 계획된 별개 작업(헌법 개정)이므로 "침범"이라기보다는 "동일 커밋에 두 개의 계획된 변경(콘텐츠+헌법)이 함께 들어간" 상태 — 각각 사용자가 명시적으로 지시한 범위와 정확히 일치.

다만 한 가지는 지적할 만하다: `docs/*.md`, `.claude/agents/*.md`가 엔진_경계.md의 세 분류표(§1/§2/§3) 어디에도 속하지 않는다는 사각지대가 이번 건으로 드러났다 — "신규 애매 항목"으로 기록 제안.

## 이식 이력 기록 제안 문안

- `docs/엔진_경계.md`에 추가할 문안(초안): "§1~§3은 실행 코드(js/css/netlify)만 다룬다. `docs/톤앤매너.md` 같은 **콘텐츠 규격 헌법 문서**와 `.claude/agents/*.md` 같은 **개발 도구 설정**은 이 표 밖에 있지만, 두 사이트가 같은 헌법·같은 서브에이전트 체계를 쓰기로 한 이상 §4 이식 절차에 준해 양쪽에 반영한다. diff 대상 파일 목록에 `docs/톤앤매너.md`, `.claude/agents/*.md`를 명시적으로 추가할지 결정 필요."
- 커밋 메시지(또는 별도 TODO)에 남길 문안: "docs/톤앤매너.md §1 개정('흔한 오해 셋' 대체 장치 예외)은 본진(AX_Lecture)에도 동일 반영 필요 — 이 커밋에는 첫걸음판만 포함, 본진 반영 여부 확인 후 별도 커밋 요함." (반영 완료 — 위 참고 항목 확인.)

# 엔진 경계 — 복제·이식의 지도

> 이 저장소를 복제해 새 사이트("AI 첫걸음" 등)를 만들기 전에 읽는 문서.
> "무엇을 그대로 가져가고, 무엇을 새로 채우는가"를 정의한다. `js/공용화_지시서.md`(order/) 산출물.

---

## 1. 공유 엔진 (양 사이트 동일 유지 — 함부로 갈라치지 않는다)

| 파일/영역 | 역할 |
|---|---|
| `js/slides.js` | 슬라이드 렌더·네비게이션·확대(zoom) 엔진 |
| `js/room.js` | 상자 4개·카드 부채꼴 전개 |
| `js/main.js` | 씬 매니저·해시 라우터·진행도/Lv/스킨 상태·유틸 레이어(🎫/🎨) 시스템 |
| `js/telemetry.js` | 수강 코드 + 이벤트 수집(fail-silent) |
| `js/admin.js` | 강사 대시보드(`#/admin`) 오버레이 |
| `netlify/functions/*` | track·stats·brief·`_shared/aggregate.js` 전체 |
| `css/style.css`의 **스켈레톤**(레이아웃·컴포넌트 셀렉터) | 스킨 값 블록을 소비하는 CSS 구조 자체 |

이 파일들을 고치는 이유가 "이 사이트만의 취향"이 아니라 "버그·구조 개선"이라면 **두 사이트 모두에 반영**하는 게 원칙(§5 이식 절차).

## 2. 사이트 고유 (복제 후 각자 채운다)

| 파일/영역 | 역할 |
|---|---|
| `js/site-config.js` | 이 사이트를 정의하는 단일 설정(course·이름·문구·스킨 기본값·fxLevel·practiceRoom 등) |
| `js/data.js` | 커리큘럼 콘텐츠(상자·강의·슬라이드) — 전량 교체 대상 |
| `assets/*.svg`, `assets/*` | 강의 도식·이미지 |
| `css/style.css`의 **스킨 값 블록**(`:root[data-skin="..."]{ --bg:...; --ink:...; }`) | 색상·타이포 변수 값 자체(구조가 아니라 값) |
| `js/intro.js`의 인트로 테마 **구성**(`site-config.js`의 `introThemes` 배열이 어떤 테마를 쓸지) | 테마 렌더 알고리즘 자체는 §3 참고 — "몇 개, 어떤 걸 쓸지"만 사이트 선택 |
| `js/desk.js` | 연습 책상(3존 실습 공간) — 이 사이트 전용 신규 기능. `PRACTICE_MISSIONS`(`js/data.js`)·`SITE_CONFIG.practiceDesk`에 결합돼 있어 본진엔 그대로 이식할 수 없다(`order/연습책상_지시서.md`) |

## 3. 경계 애매 항목과 판단 기준

완전히 한쪽으로 나뉘지 않는 것들 — 복제 시점에 실제로 판단이 필요하다.

- **`js/practice.js`(진단실)**: 스테이션 내비게이션·시험 엔진·캔버스 레이더 같은 뼈대는 재사용 가치가 높지만,
  문항 내용(`LIT_Q`/`NAT_Q`/`TAC_Q`/`AXES`/`STATIONS`의 텍스트)은 AX 커리큘럼에 강하게 결합돼 있다.
  `SITE_CONFIG.practiceRoom=false`로 기능 자체를 끌 수는 있게 해뒀지만(진입 버튼·`#/practice` 라우트 모두 차단),
  **진단실을 켠 채로 새 사이트를 만든다면 이 파일은 사실상 "공유 뼈대 + 사이트 고유 문항"의 혼합 파일**이 된다 —
  복제 시 문항을 통째로 다시 쓸지, 뼈대만 추출해 별도 모듈로 뺄지는 그때 가서 판단.
- **`"사유의 방"` 스킨 이름**: `SITE_CONFIG.siteName`과 우연히 같은 문자열이지만, 스킨 카탈로그의 표시명
  (`skin-lb`)은 이번 리팩터에서 SITE_CONFIG로 옮기지 않고 그대로 두었다. 사이트명과 스킨명이 항상 같이
  가야 하는 건 아니므로, 복제 사이트가 이 스킨을 그대로 쓴다면 이름을 따로 정할지 결정 필요.
- **입장 화면 안내문("사유의 상자 하나를 열어보세요" 등)**: "상자" 메타포 자체는 `data.js`의
  `CURRICULUM.boxes` 구조(엔진 개념)와 맞닿아 있지만, 문장의 색깔("사유의")은 사이트 고유에 가깝다.
  이번 리팩터에서는 site-config로 옮기지 않았다(범위 조절) — 다음 정리 때 후보.
- **인트로 테마 렌더 로직(`js/intro.js`의 `THEMES` 객체 — 별똥별/벚꽃/불꽃놀이/함박눈 그리기 알고리즘)**:
  코드 자체는 공유 엔진이지만, `#intro-grid`의 4분할 버튼(`index.html`)은 아직 **정적 마크업**이라
  테마 개수를 바꾸려면(예: 3개로 축소, 5번째 테마 추가) `index.html`도 함께 고쳐야 한다 —
  `introThemes` 배열만으로는 완전히 끝나지 않는다.
- **Supabase 프로젝트를 공유할지 분리할지**: `course` 컬럼으로 한 프로젝트 안에서 여러 사이트를
  구분하도록 만들어뒀다(§6). 그러나 이는 기본값일 뿐 — 완전히 별도 계정/프로젝트로 분리하는 것도
  선택지다(장점: 사고 격리·독립 요금제. 단점: 강사 대시보드를 둘 다 봐야 하면 로그인을 두 번 해야 함).
- **`slidesVariants`(이 저장소) vs `slidesPersonal`(본진) — 페르소나/경험판 메커니즘 비대칭**: 이
  저장소는 페르소나별(김주임/박선배/최사장) 슬라이드 인덱스 단위 부분 오버라이드(`slidesVariants`)를
  쓰고, 본진(AX_Lecture)은 07·08강 한정으로 강 전체를 통째로 교체하는 경험판(`slidesPersonal`)을
  쓴다. 렌더 분기 로직(`App.resolveSlides`)은 `js/main.js`·`js/slides.js`(§1 공유 엔진)에 있지만
  두 저장소의 실제 구현은 이미 구조적으로 갈라져 있다 — 이 저장소 쪽이 본진의 `slidesPersonal`
  예약어 처리를 흡수한 상위 호환 형태(`docs/페르소나_작성_가이드.md` "본진 이식 메모" 참고). 본진
  으로의 마이그레이션 필요성은 낮게 평가됐다(본진에 페르소나 콘텐츠 수요가 없음) — 실행 여부·시점은
  본진 `order/보류항목_AX.md`에서 별도 결정 중이며, 복제·이식 작업 시 "전체 교체(slidesPersonal)"와
  "부분 오버라이드(slidesVariants)" 중 어느 쪽을 쓸지는 그때 가서 판단한다. (개정: 본진
  `order/AX소급검증_4라운드_지시서.md` §① — 본진 §3에만 있던 이 항목을 양쪽 동기화 차원에서
  이 저장소에도 소급 반영.)

## 4. 공유 규범 (Shared Norms) — 코드는 아니지만 양쪽이 같아야 하는 것

boundary-keeper 첫 실전 가동에서 발견된 사각지대: `docs/*.md`·`.claude/agents/*`는 §1(공유 엔진)에도
§2(사이트 고유)에도 안 걸린다. 코드가 아니라서 "고치면 두 사이트 모두 반영"이라는 §1 원칙이 자동으로
적용되지 않지만, 실제로는 두 사이트가 같은 규범 위에서 움직여야 하는 문서들이라 별도 분류가 필요하다.

| 파일/영역 | 역할 |
|---|---|
| `docs/UX_원칙.md` | UI 원칙(모바일 우선·레이어 패턴 등) |
| `docs/엔진_경계.md` | 이 문서 자체 — 복제·이식의 지도 |
| `docs/에이전트_운영.md` | 서브에이전트 5종 운영 가이드 |
| `.claude/agents/*` | 서브에이전트 정의(persona-reviewer·tone-keeper·qa-runner·boundary-keeper·release-manager) |
| `tests/` 공통 골격 | `aggregate.test.js`·`functions-contract.test.js` 같은, 공유 엔진 파일을 그대로 테스트하는 스펙(사이트별 차이가 없는 부분) |

**성격**: 코드가 아니지만 **양쪽 저장소가 동일해야 하는 규범**이다. 한쪽에서 개정하면 다른 쪽에
동기화한다 — §1 공유 엔진과 같은 취급이되, "실행되는 코드"가 아니라 "지켜야 할 약속"이라는 점만 다르다.

**예외(사이트별로 다를 수 있는 규범 — 동기화 대상이지만 사이트별 조정 허용)**:
- `docs/톤앤매너.md` — 공통 구조(§ 번호 체계·조문 형식)는 동기화하되, 규격 수치(장수 범위)·도식 톤·
  어투 같은 내용은 사이트별로 다르다(첫걸음판/AX판).
- `docs/운영자_치트시트.md` — 스위치 설명 등 공통 골격은 동기화하되, 그 사이트에 없는 기능(예: 첫걸음의
  진단실 미보유) 관련 절은 사이트별로 뺄 수 있다.
- `tests/content-lint.test.js`의 **린트 상수**(`LECTURE_COUNT`, `VALID_SLIDE_TYPES` 등)와
  사이트 고유 구조 검사(예: 본진의 `slidesPersonal` 검사 블록) — 테스트 골격은 동기화하되 상수·
  사이트 전용 블록은 각자 조정.
- `.claude/agents/tone-keeper.md`의 §2(어투)·§3(용어) 관련 세부 서술, `.claude/agents/
  persona-reviewer.md`·`qa-runner.md`의 페르소나/variant·진단실 관련 표본 항목 — 두 사이트가
  실제로 다른 메커니즘(첫걸음: 다중 페르소나 `slidesVariants` / 본진: 단일 관점+`slidesPersonal`
  경험판+진단실, 첫걸음은 진단실 없음)을 쓰기 때문에 생기는 서술 차이는 정당하다. 단 이 파일들의
  **다른 부분**(규격·금지사항·검사 절차 골격 등)은 여전히 동기화 대상이다 — "이 파일 전체가 예외"가
  아니라 "이 파일 안의 특정 구획만 예외"임에 유의(개정: 본진 `order/AX검증_마감_지시서.md` ③ —
  마감 전 규범 동기화 검사에서 이 목록에 `.claude/agents/*`가 아예 빠져 있어 §7 로그가 근거 없이 이
  예외를 인용해온 구조적 공백을 발견, 본진→이 저장소로 소급 반영, 명시적으로 등재).

**절차**: 이 분류의 파일을 개정할 때는 ① 어느 쪽 저장소에서 먼저 개정했는지, ② 상대 저장소에 언제·
어떻게 반영했는지(또는 반영 보류 사유)를 커밋 메시지나 이 문서에 이력으로 남긴다.

## 5. 이식 절차 — "AI 사이트에서 고친 엔진 버그/개선을 본진(AX)에 반영"

새 사이트에서 §1(공유 엔진) 파일을 고쳤다면, 그 개선은 본진에도 반영한다(본진이 "엔진 원본" 지위를 유지).

1. AI 사이트 저장소에서 수정.
2. AI 사이트 로컬(`netlify dev`)에서 아래 **검증 최소 세트** 통과.
3. 같은 변경분을 본진(AX, 이 저장소) `feature/*` 브랜치에 수동 적용(파일이 갈라졌다면 `git diff`로 대조
   후 patch, 아직 안 갈라졌다면 `git cherry-pick`도 가능).
4. 본진에서도 **검증 최소 세트** 재통과 확인 후 커밋·병합.

**diff 대상 파일** (이 목록 밖의 파일이 걸려 있다면 "엔진 개선"이 아니라 "사이트 고유 변경"이 섞였을 가능성 —
멈추고 분리):
`js/slides.js` · `js/room.js` · `js/main.js` · `js/telemetry.js` · `js/admin.js` · `js/vendor/qrcode.js` ·
`netlify/functions/track.js` · `netlify/functions/stats.js` · `netlify/functions/brief.js` ·
`netlify/functions/codes.js` · `netlify/functions/_shared/aggregate.js` ·
`css/style.css`(스킨 값 블록 제외 — 셀렉터/레이아웃만)

**검증 최소 세트** (docs/UX_원칙.md 체크리스트와 함께):
- [ ] 입장·강의 진행·진단실(켜져 있다면)·스킨 전환·이어보기·`?variant=` 표본 회귀 없음
- [ ] `#/admin` 진입·매트릭스·진단 집계·📋 주간 브리핑 정상
- [ ] 375px 레이아웃 안 깨짐, 콘솔 0건, 키 비노출

## 6. 복제 절차 — 새 사이트 만들기

1. 이 저장소를 복제(git clone 또는 템플릿으로 새 리포 생성).
2. **`js/site-config.js` 전체 값 교체** — `course`부터 `finalMessage`까지. `course`는 짧은 영소문자
   식별자로(예: `'aifirst'`) — Supabase `events.course` 값과 동일해야 한다.
3. **`js/data.js` 콘텐츠 전량 교체** — 새 커리큘럼(상자·강의·슬라이드).
4. **스킨**: `css/style.css`의 스킨 값 블록을 그대로 쓸지, 새 팔레트로 바꿀지 결정 후 교체.
   `SITE_CONFIG.availableSkins`/`defaultSkin`이 실제 정의된 스킨과 어긋나지 않는지 확인.
5. **`assets/`** 도식(SVG 등)을 새 콘텐츠에 맞게 교체.
6. **Supabase는 기본적으로 공용**(§3 참고) — 같은 프로젝트를 쓴다면 `.env`의
   `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`는 두 사이트가 동일한 값을 쓰고, `SITE_CONFIG.course`만
   다르게 설정하면 `events` 테이블 하나에 두 과정 데이터가 `course`로 구분돼 쌓인다.
   `ADMIN_KEY`·`AI_API_KEY`는 사이트별로 새로 발급하는 것을 권장(강사 대시보드 접근을 분리하고 싶다면).
7. **Netlify**: 새 사이트용 Netlify 프로젝트를 새로 만들어 이 저장소(복제본)를 연결, 환경변수 4종
   (`SUPABASE_URL`·`SUPABASE_SERVICE_KEY`·`ADMIN_KEY`·`AI_API_KEY`) 등록. `docs/URL전환_체크리스트.md` 참고.
8. **검증**: §5의 검증 최소 세트 + docs/UX_원칙.md 체크리스트 전체 실행.

## 7. 이식 이력

§5 절차를 실제로 거쳐 본진에 반영된 엔진 개선 기록(§4 공유 규범 동기화 이력도 여기 함께 기록).
새 이식이 생기면 여기 추가.

> **소급 기록 안내**: 이 §7 섹션은 boundary-keeper가 `order/보안게이트_슬라이드_지시서.md` 검증
> 가동 중 "본진(AX_Lecture) `docs/엔진_경계.md`엔 §7이 있는데 이 저장소엔 없다"는 구조적 공백을
> 발견해 신설됐다(2026-07-22). 아래 네 건은 실제로는 그 이전에 있었던 반영이지만, 기록할
> 자리가 이 저장소에 없어 지금 소급 기재한다 — 본진 `docs/엔진_경계.md` §7의 대응 항목과 내용 일치.

### 2026-07-21 — AI_First_Step → AX_Lecture (본진으로 이식), SITE_CONFIG fallback 4건 (소급 기록)

- **출처**: 이 저장소(AI_First_Step) `de5d5a6`("첫 출근" 복제 작업 중 발견) → 본진(AX_Lecture)
  `695969f`("엔진 백포트 1차: config fallback 4건 + .pdoor hidden 버그픽스")로 이식.
- **적용 파일**: 본진의 `js/main.js`·`js/room.js`·`js/intro.js`·`css/style.css`(이 저장소 파일은
  변경 없음 — 발견만 이 저장소에서 나오고 반영은 본진에 함).
- **내용**: 전부 `SITE_CONFIG.값 || 본진 기본값` fallback 패턴 4건.
  1. `js/main.js` — `skinGating` 우회 · `rankTitles` 칭호 · `completionTitle`/`completionTitleMastered`
     완주 문구.
  2. `js/room.js` — 상자 라벨(`boxLabel`) fallback.
  3. `js/intro.js` — 인트로 기본 테마(`introThemes[0]`) fallback.
  4. **버그픽스**: `css/style.css`의 `.pdoor`(진단실 입장 버튼)에 `[hidden]{display:none}` 가드가
     빠져 있어, 본진에서 Lv2 미만 사용자에게도 진단실 버튼이 실제로 보이고 있던 문제를 본진에서
     재현 확인 후 수정. 이 저장소는 `practiceRoom:false`라 이 버그의 영향을 받지 않았음(발견 경위만
     이 저장소 작업 중 나왔다).
- **검증**: 본진에서 회귀 0(스킨 Lv 게이팅·칭호 5종·완주 문구·상자 라벨·인트로 4테마 기본값 전부
  이전과 동일), Lv1 진단실 미노출/Lv2·Lv5 정상 노출, 콘솔 0건 확인 완료(본진 `docs/엔진_경계.md`
  §7 해당 항목 참고). 상세 내용은 본진 문서가 정본.

### 2026-07-22 — 공유 규범(§4) 신설, 첫걸음 ↔ 본진 동시 반영

- **출처**: 이 저장소, boundary-keeper 미팅룸 검증 가동에서 발견(`docs/*.md`·`.claude/agents/*`가
  §1/§2 분류표 밖에 있다는 사각지대).
- **적용 파일**: `docs/엔진_경계.md`(이 문서 — §4 신설 + §4 이후 번호 재정렬), `.claude/agents/
  boundary-keeper.md`(공유 규범 분류·동기화 플래그 검사 단계 추가).
- **내용**: 두 저장소에 같은 시점에 같은 내용으로 반영 — 방향성 있는 "이식"이 아니라 최초 동시 도입.
- **검증**: 두 저장소 문서의 §번호·조문 내용 일치(수동 대조). `boundary-keeper.md`는 두 저장소에서
  바이트 단위로 동일(diff 0).

### 2026-07-22 — 톤앤매너 헌법에서 현황 스냅샷 분리, 첫걸음 ↔ 본진 동시 반영

- **출처**: 이 저장소, `order/내책상발견_조치_지시서.md` §③(tone-keeper 첫 가동에서 헌법 §8
  스냅샷이 매 작업마다 낡는다는 문제 발견에 대한 조치).
- **적용 파일**: `docs/톤앤매너.md`(§8 현황 스냅샷 절 삭제, §9→§8 재정렬), `docs/콘텐츠_현황.md`
  (신설), `.claude/agents/tone-keeper.md`(현황 참조를 `docs/콘텐츠_현황.md`로 안내하는 문구 추가).
- **내용**: 두 저장소에 같은 시점에 같은 구조로 반영. `콘텐츠_현황.md`의 **내용**은 사이트별로 다르다
  (사실 자체가 다르므로 — §4 "사이트별 조정 허용 예외"에 준하는 성격).
- **검증**: 두 저장소 `docs/톤앤매너.md`의 §번호 체계 일치(수동 대조). `tone-keeper.md`는 두 저장소에서
  바이트 단위로 동일(diff 0).

### 2026-07-22 — tone-keeper 판정 대상 범위 조항 명문화, 첫걸음 ↔ 본진 동시 반영

- **출처**: 이 저장소, `order/보안게이트_슬라이드_지시서.md` §⑥(내 책상 검증에서 tone-keeper가
  "완료 상태만 판정 대상으로 삼는다"는 절차를 스스로 유추해 적용했으나 조문에는 없다고 제안한 것을
  명문화).
- **적용 파일**: `docs/톤앤매너.md`(§7에 "판정 대상 범위" 조항 신설), `.claude/agents/
  tone-keeper.md`("0단계"로 현황 문서 선(先) 조회 절차 명문화 + 불일치 발견 시 높음 심각도 보고
  규칙 추가).
- **내용**: 두 저장소에 같은 시점에 같은 문구로 반영.
- **검증**: 두 저장소 `docs/톤앤매너.md` §7 조항 문구 일치(수동 대조). `tone-keeper.md`는 두 저장소에서
  바이트 단위로 동일(diff 0).

### 2026-07-22 — 캡스톤 강 예외 + 의미색 예외, 첫걸음 ↔ 본진 동시 반영

- **출처**: 이 저장소, `order/보안게이트발견_조치_지시서.md` §①②(20강 "흔한 오해 셋" §1 위반·
  E6 신호등 팔레트 이탈에 대한 tone-keeper 발견에 대한 사람 결정).
- **적용 파일**: `docs/톤앤매너.md`(§1에 캡스톤 강 예외 신설, §5에 의미색 예외 신설, §7 판정표
  두 항목 반영), `.claude/agents/tone-keeper.md`(캡스톤 판정 순서·의미색 예외 판정 기준 추가).
- **내용**: 두 저장소에 같은 시점에 같은 문구로 반영 — 본진 20강도 동일한 캡스톤 성격이라 실질적으로
  필요한 조항.
- **검증**: 두 저장소 `docs/톤앤매너.md` §1·§5·§7 문구 일치(수동 대조). `tone-keeper.md`는 두
  저장소에서 바이트 단위로 동일(diff 0).

### 2026-07-23 — 규범 동기화 갭 해소 + 절차 보강, 본진 → 이 저장소로 소급 반영

- **출처**: 본진(AX_Lecture) `order/AX소급검증_4라운드_지시서.md` ① — 본진 3라운드
  boundary-keeper가 "diff는 봤지만 현재 상태 전수 대조는 안 했다"는 검사 방법론의 구멍을 발견,
  본진에서 양쪽 전수 diff를 뜬 뒤 이 저장소로 반영.
- **이 저장소에 반영된 갭 해소**:
  1. `.claude/agents/tone-keeper.md`에 본진 "배열이 정본" 판정 원칙(본진 1라운드 반영)을
     동기화 — "규격 준수" 항목에 동일 문구 추가.
  2. `docs/엔진_경계.md`(이 문서) §3에 "`slidesVariants`(이 저장소) vs `slidesPersonal`(본진)"
     항목 신설 — 본진 §3에만 있던 대응 항목을 이 저장소 시점으로 새로 서술.
  3. `docs/에이전트_운영.md` "하네스와의 관계" 절이 낡아 있었음(이 저장소 하네스는 이미 완성돼
     있었는데 표준 파이프라인 텍스트만 "하네스 등록 후" 표현으로 남아 있었음) — 실제 상태(구성
     3종·실행법·탐지력 검증 이력)로 갱신.
  4. `docs/에이전트_운영.md` 표준 파이프라인의 "qa-runner (+ 하네스 등록 후: npm test 포함)"를
     "qa-runner (0단계: npm test)"로 정정 + "(마일스톤 마감 전) 규범 동기화 검사" 단계 삽입.
- **절차 보강**: `.claude/agents/boundary-keeper.md`에 6단계 "규범 동기화 상태 검사" 신설(본진과
  동일 문구) — 양쪽 저장소 `boundary-keeper.md`는 이 갱신 후에도 바이트 단위로 동일(diff 0) 유지.
- **검증**: 본진에서 위 갭 전부 대조 완료(재대조 diff 0 또는 의도된 자기-시점 서술로 확인). 상세
  내용은 본진 `docs/엔진_경계.md` §7 해당 항목이 정본.

### 2026-07-23 — 헌법 §3 약어 조문 삼분, 본진 → 이 저장소로 소급 반영

- **출처**: 본진(AX_Lecture) `order/AX검증_마감_지시서.md` ① — 본진 소급 검증 종합보고의 용어
  풀이 관련 권고를 마감 단계에서 양쪽에 실행.
- **적용 파일**: `docs/톤앤매너.md` §3(기존 "05강 10종" 고정 용어집 체계는 그대로 유지하되, 그
  10종 밖의 약어를 경영·업무 지표 약어/관용 로마자 표기/기술 개념 용어로 삼분해 반영 — 본진과
  구조는 동일하나 ③-1에 "입문 과정인 이 사이트는 첫 등장 시 풀이 필수"라는 이 사이트 고유 특례가
  붙음, §7 판정표 갱신), `.claude/agents/tone-keeper.md`(용어 규칙을 §3 삼분 기준으로 갱신).
- **내용**: 구조는 본진과 동일하게 반영하되, ③-1의 실제 적용 결과는 사이트별로 다르다(본진=경영
  약어 무풀이가 기본, 이 사이트=입문 과정이라 첫 등장 시 풀이 필수) — §4 "사이트별 조정 허용"
  예외에 해당하는 자연스러운 비대칭.
- **검증**: 본진과 §3 조문 구조·번호 일치(수동 대조). `tone-keeper.md`의 §3 관련 서술은 각 사이트
  고유 규칙(05강 10종·입문 특례 vs 경험판 조항)이 있어 완전 동일은 아니나, 상호 참조 정합 확인.

### 2026-07-23 — §4 예외 목록에 `.claude/agents/*` 명시 등재 + §8 스펙 오기 정정, 본진 → 이 저장소로 소급 반영

- **출처**: 본진(AX_Lecture) `order/AX검증_마감_지시서.md` ③ — 본진 마감 전 규범 동기화 검사에서
  발견된 두 갭을 이 저장소에도 동일하게 반영.
- **적용 파일 1**: `docs/엔진_경계.md` §4 예외 목록에 4번째 불릿 신설(본진과 동일 취지) —
  `tone-keeper.md`의 §2/§3 세부, `persona-reviewer.md`·`qa-runner.md`의 페르소나/variant·진단실
  관련 표본 항목은 두 사이트 메커니즘 차이로 인한 정당한 서술 차이이되, 이 파일들의 다른 부분은
  여전히 동기화 대상임을 명시.
- **적용 파일 2**: `docs/톤앤매너.md` §8("본진(AX) 판과의 차이")의 "규격: 강당 15~17장"이 오기임을
  발견 — 1라운드 소급 검증에서 이미 13~17장으로 정정된 사실이 이 사이트의 §8 교차 참조 문구에는
  반영되지 않고 남아 있었다. 13~17장으로 정정.
- **검증**: 본진 `docs/톤앤매너.md` §1(13~17장 원문)과 대조 확인.

### 2026-07-23 — 게이미피케이션 Lv3 개편: 레벨 상한·스킨 게이팅 엔진을 site-config 주도로 재작성, 본진 → 이 저장소 반영

- **출처**: 본진(AX_Lecture) `order/게이미피케이션_Lv3_지시서.md` — 사용자 실사용 검토에서 나온
  수정 요청("레벨 상한·스킨 게이팅은 site-config 주도로 구현, 엔진 하드코딩 금지").
- **적용 파일**: `js/main.js`(§1 공유 엔진, 본진과 동일 구조로 반영).
  1. `const MAX_LEVEL = 5;`(하드코딩) → `SITE_CONFIG.maxLevel || 5`로 변경. 기존 `Level.load()`의
     클램프 로직이 그대로 있어, 상한을 낮추기만 해도 기존 localStorage의 구 Lv가 로드 시 자동으로
     새 상한에 클램프된다 — 별도 마이그레이션 코드 불필요.
  2. 스킨 해금 판정을 "PC는 Lv별로 배열 순서대로 하나씩" 방식에서 `SITE_CONFIG.skinUnlockLevel`
     (스킨명→필요 Lv 명시적 맵) 기반으로 교체 — 한 Lv에 여러 스킨이 동시 해금되는 걸 지원해야 했다
     (이 사이트: Lv1에 오피스+페이퍼 동시 해금 — 기존 1:1 순서 매핑으로는 표현 불가능했음).
  3. `refreshSkinLocks()`의 "LvN" 뱃지 계산도 새 `skinUnlockLevel()` 조회로 교체.
- **사이트별 값(site-config.js, 동기화 대상 아님 — §4 예외)**: `maxLevel:3`, `rankTitles:["","인턴",
  "주임","에이스"]`(기존 5단계에서 압축), `skinGating:false` 삭제(게이팅 없음 정책 폐기 — 사용자
  결정), `availableSkins`에 신규 `sunset` 추가, `skinUnlockLevel:{office:1,paper:1,neon:2,sunset:3}`.
- **신규 스킨(이 사이트 전용, 사이트 고유 콘텐츠)**: `css/style.css`에 `:root[data-skin="sunset"]`
  블록 신설("첫 출근을 마치고 퇴근길에 보는 노을" — 오피스(아침)→노을(저녁) 하루 서사, 다크 남색~
  보라 배경+앰버/코럴 포인트). `js/main.js`의 배경 캔버스 `draw()`에 `sunset` 분기 추가(별 1~2점만
  정적으로, `fxLevel:"calm"`에 맞춰 절제). `index.html` 스킨 선택기에 버튼 추가.
- **AX 전용 변경(사이트 고유, 이 저장소엔 미적용)**: `#practice-door` 버튼 부제 제거는 본진의
  진단실 UI에만 해당(이 사이트는 `practiceRoom:false`로 애초에 해당 없음).
- **검증**: 로컬 서버에서 legacy localStorage(Lv3·구 스킨 저장) 시뮬레이션 → 로드 시 정상 클램프
  확인. Lv1/2/3 전 구간 스킨 잠금·뱃지·업그레이드·통달 연출(완주 문구 "이제 이 사무실의
  에이스입니다." 포함) 확인. sunset 스킨 CSS 변수·배경 캔버스 정상 렌더, 콘솔 0건. 375px 오버플로
  없음. 도식(E 시리즈) 렌더는 기존 `neon`(다크 스킨)과 동일한 방식이라 신규 이슈 아님.

### 2026-07-23 — 연습 책상 신설, §1 공유 엔진 파일 확장분은 백포트 보고만(실행 안 함)

- **출처**: 이 저장소, `order/연습책상_지시서.md` — 이 사이트 전용 3존 실습 공간(미션 스탬프판·
  프롬프트 조립대·나의 시작 키트) 신설.
- **사이트 고유 신규**: `js/desk.js`, `js/data.js`의 `PRACTICE_MISSIONS`, `js/site-config.js`의
  `practiceDesk:true`, `index.html`의 `#desk-door`/`#scene-desk`, `css/style.css`의 연습 책상
  전용 셀렉터(§2 표에 `js/desk.js` 행으로 등재).
- **§1 공유 엔진 파일을 건드린 부분(백포트 후보로 보고만 — 이번 라운드는 실행하지 않음)**:
  `js/telemetry.js`(이벤트 API에 `missionStamp`·`kitUpdated` 2건 추가), `netlify/functions/
  track.js`(`EVENT_TYPES`에 `mission_stamp`·`kit_updated` 2건 추가), `netlify/functions/_shared/
  aggregate.js`(`summarizeStudent`에 `missionCount`·`kit` 집계 추가), `js/admin.js`(수강생 상세에
  "연습 책상" 한 줄 추가), **`js/main.js`(씬 매니저에 `scene-desk` 참조, 해시 라우터에 `case
  "desk"` 추가 — `SITE_CONFIG.practiceDesk`로 게이트돼 있어 본진에 그대로 있어도 no-op)**. 이
  확장분은 전부 `PRACTICE_MISSIONS`/연습 책상 데이터에 의존하므로 본진(AX_Lecture)에 그대로
  이식하면 죽은 코드가 된다 — 본진에 연습 책상과 대응하는 기능이 생기기 전까지는 백포트 보류가
  합리적이라고 판단했다. 지시서 자체가 "AI_First_Step에서 실행"으로 범위를 좁혔고 "엔진 공용부를
  건드리면 §7 이식 대상 보고"라고 명시해, 실행이 아니라 보고로 마무리한다. 같은 이유로
  `tests/content-lint.test.js`(§4 예외 — `PRACTICE_MISSIONS` 사이트 고유 구조 검사)와
  `tests/functions-contract.test.js`(§4, mission_stamp/kit_updated/stats 계약 테스트 3건)도
  함께 확장했으나 본진 백포트는 보류한다(boundary-keeper 재검토로 §1 목록에서 `js/main.js`
  누락·§4 두 테스트 파일 미기재를 발견해 이 문단에서 보완). 실제 이식 여부·시점은 본진에 유사
  기능이 필요해질 때 사람이 결정.
- **검증**: `js/telemetry.js`/`netlify/functions/*`의 기존 이벤트 5종(session_start 등) 동작은
  무변경 확인(추가만 있고 기존 로직 수정 없음). `npm test` 133/133 통과(신규 7건 포함).
  boundary-keeper 재검토: 16개 변경 파일 전수 diff 확인, 목록 밖 파일 없음, §1 파일 전부 순수
  추가(기존 로직 삭제·변경 없음) 확인.

### 2026-07-23 — LMS 운영 기초(코드 발급·검증) 신설, 본진 주도·§1 공유 함수만 동기 반영

- **출처**: 본진(AX_Lecture) `order/LMS_운영기초_지시서.md` — "가장 기본만"(관리 화면·이름 매핑·
  코드 비활성화·수강생 계정 없음). 본진에서 실행, 이 저장소는 §1 공유 엔진 함수만 동기 반영
  ("첫걸음은 동기화 확인만" — 지시서 자체가 범위를 좁힘).
- **신규 테이블**: `codes`(code PK·course·batch·created_at, RLS 기본 거부, `docs/supabase_setup.sql`)
  — 본진과 같은 Supabase 프로젝트를 쓰므로 테이블은 이미 한 번만 만들면 양쪽에 적용됨.
- **신규 함수(§1 공유 엔진)**: `netlify/functions/codes.js` — 본진과 바이트 동일. GET(발급된 반
  목록)·POST(반 단위 코드 발급, 재요청 시 기존 코드 그대로 반환 — 재발급 안전).
- **`netlify/functions/track.js` 변경(§1 공유 엔진)**: events insert 직전 `codes` 테이블 조회 추가
  — 미등록 코드는 204로 조용히 무시. 본진 코드를 그대로 복사해오면 이 저장소 고유 `EVENT_TYPES`
  (`mission_stamp`·`kit_updated`, 위 "연습 책상" 항목에서 추가된 것)가 빠질 뻔했다 — 반영 직후
  자체 diff 검토로 발견·복구(커밋에는 정상 상태로 반영, 연습 책상 이벤트 계속 정상 동작).
- **`js/telemetry.js` `normalize()` 확장(§1 공유 엔진)**: 본진과 동일하게 전각→반각·트림 추가.
  이 저장소 고유의 `missionStamp`/`kitUpdated`/persona-variant payload 로직은 그대로 유지, 이
  함수만 교체.
- **QR 라이브러리 벤더링(§1 공유 엔진, 양쪽 바이트 동일)**: `js/vendor/qrcode.js` — 본진과 동일
  (kazuhikoarase/qrcode-generator MIT, `js/dist/qrcode.js` 그대로).
- **`js/admin.js` — 코드 발급 UI(§1 공유 엔진)**: `#/admin` 헤더에 **[🎫 코드 발급]** 버튼 + 레이어.
  이 저장소 고유의 수강생 상세 "연습 책상" 한 줄(위 항목에서 추가된 것)과는 다른 위치라 충돌 없음.
- **적용 파일**: `docs/supabase_setup.sql`, `netlify/functions/codes.js`(신규), `netlify/functions/
  track.js`, `js/telemetry.js`, `js/vendor/qrcode.js`(신규), `js/admin.js`, `css/style.css`,
  `index.html`, `tests/functions-contract.test.js`(codes 계약 테스트 8건 + track 등록/미등록
  코드 테스트 2건 추가, 기존 mission_stamp/kit_updated 테스트는 codes 게이트에 맞게 stub 보강만).
- **검증**: `npm test` 142/142 통과(기존 133 + 신규 관련분). 본진 쪽에서 실기 검증(발급→재발급
  멱등성→CSV→QR 카드→등록/미등록 코드 실제 insert 여부→전각 정규화)을 이미 마쳤고 동일 코드를
  그대로 반영한 것이므로, 이 저장소는 `node --check` 구문 검사 + 전체 테스트 통과로 동기화 확인을
  마무리(지시서가 명시한 "동기화 확인만" 범위).

### 2026-07-23 — qa-runner 시각 검증 프로토콜 신설(공유 규범), 첫걸음 ↔ 본진 동시 반영

- **출처**: 이 저장소, `order/시각검증체계_지시서.md` — 연습 책상이 기능 검증(DOM·콘솔)은 전부
  통과했으나 실제 화면 배치가 깨진 채(헤더가 좌측에 고립, 카드가 우측 좁은 컬럼에 쏠림) 완료
  보고된 사고에서 출발. 근본 원인은 `.scene` 기본값(`display:flex;align-items:center;
  justify-content:center` — 단일 위젯 중앙정렬용)에 대해 `#scene-practice`는 `display:block`
  오버라이드가 있었는데 `#scene-desk`엔 그 오버라이드가 없어, `.pr-top`(헤더)·`.pr-inner`(그리드)가
  플렉스 형제로 나란히 배치돼버린 것 — `css/style.css`에 `#scene-desk{display:block;
  overflow-y:auto}` 한 줄 추가로 해결(1440/768/375px 3뷰포트 `getBoundingClientRect` 측정으로
  구조 정합 확인 — 단 실제 스크린샷 캡처는 이 세션에서 실패, 아래 참고).
- **적용 파일(공유 규범, §4)**: `.claude/agents/qa-runner.md`(3단계로 "시각 검증(필수)" 신설 —
  375/768/1440px 스크린샷 캡처, 캡처 실패 시 DOM 대체 판정 금지 + `⚠ 시각 검증 미수행` 플래그
  의무화, 출력 표에 "시각 검증" 행 추가), `docs/에이전트_운영.md`(표준 파이프라인 설명에 동일
  내용 반영), `docs/UX_원칙.md`(점검 체크리스트에 "3뷰포트 스크린샷 확인" 항목 추가).
- **내용**: 두 저장소에 같은 시점에 같은 문구로 반영(qa-runner.md는 각 저장소의 기존 사이트별
  차이 — 스킨 5종 vs 4종, `?variant=personal`/진단실 표본 행 등 — 는 그대로 유지하고 새 절차만
  추가).
- **⚠ 이 세션 자체의 한계**: 이번 라운드에서 시각 검증을 실제로 수행하려 했으나, Browser 패널이
  사용자 쪽에 표시되지 않아 `computer` 스크린샷이 매번 타임아웃(`the Browser pane is not
  displayed, so the page is not compositing frames`)됐다. 새 프로토콜이 요구하는 대로 DOM 기하
  측정(`getBoundingClientRect`)을 **보조 증거로만** 남기고 "통과"로 표시하지 않았다 — 사용자에게
  `⚠ 시각 검증 미수행 — 사용자 육안 확인 필요`를 명시적으로 전달한다.
- **검증**: 두 저장소 `.claude/agents/qa-runner.md`·`docs/에이전트_운영.md`·`docs/UX_원칙.md`
  신규 문단 문구 일치(수동 대조). 각 저장소 고유 표(스킨 목록·표본 행 등)는 의도적으로 다름.

### 2026-07-23 — 수강 식별 방식 개편(반 코드+이름) + 서버 상시 무충돌 설정, 본진 주도·반영

- **출처**: 본진(AX_Lecture) `order/코드방식_서버설정_지시서.md` — 사용자 결정 2건: ① 개인별
  코드·QR 카드 발급 → **반 코드 1개 + 학생이 이름 직접 입력**으로 전환, ② 본진·첫걸음 두 로컬
  서버를 어떤 순서로 띄워도 **기본값으로 충돌 없게** 포트 고정.

**A. 수강 식별 방식(§1 공유 엔진, 본진과 동일 구조로 반영)**
- `index.html` 🎫 내 정보 레이어: 입력칸 1개(코드)→2개(코드+이름). 저장 식별자는 여전히 `code`
  자리 하나에 `{반코드}-{이름}`로 합성해 넣는다 — 대시보드·이벤트 스키마 구조는 전혀 안 바뀐다.
- `js/telemetry.js`: `normalize()`를 `normalizeCode()`(기존과 동일)와 `normalizeName()`(신규,
  내부 공백까지 제거)으로 분리. `setCode(rawCode, rawName)`으로 시그니처 변경. 이 저장소 고유의
  `missionStamp`/`kitUpdated`/persona-variant payload 로직은 그대로 유지, 이 함수들만 교체.
- `netlify/functions/track.js`: 등록 판정을 exact match → **prefix match**로 변경(식별자가 등록된
  반코드 중 하나 + `"-"`로 시작하는지). 이 저장소 고유 `EVENT_TYPES`(`mission_stamp`·`kit_updated`)
  는 그대로 유지. 식별자 길이 상한을 20→51로 완화.
- `netlify/functions/codes.js`: 개인별 코드 다수 발급(count) → **반 코드 1개 등록**으로 전면
  단순화(본진과 바이트 동일 — 이 함수엔 사이트 고유 확장이 없었음).
- `js/admin.js` 코드 발급 UI: 인원수 입력·CSV·개인 카드 그리드 폐지, 반코드 큰 글씨 + QR 1개 +
  "반 안내 1장" 인쇄로 단순화. 이 저장소 고유의 "연습 책상" 수강생 상세 표시 줄과는 다른 위치라
  충돌 없음.
- **대시보드는 무변경으로 자동 대응**(본진과 동일 이유).
- **적용 파일**: `index.html`, `js/telemetry.js`, `netlify/functions/track.js`, `netlify/functions/
  codes.js`, `js/admin.js`, `css/style.css`, `tests/functions-contract.test.js`(codes 계약
  재작성 + track 접두매칭·51자 경계·부분일치 오탐 방지 테스트 추가, 기존 mission_stamp/kit_updated
  테스트는 새 반코드 형식에 맞게 stub 값만 보정), `docs/LMS_운영매뉴얼.md`(전면 개정), `docs/
  운영자_치트시트.md`.

**B. 서버 상시 무충돌 설정(사이트 고유 값, 구조는 동일)**
- `netlify.toml`에 `[dev]` 블록 신설 — 이 저장소 `port=8001, staticServerPort=3998`(본진은
  8000/3999). 그 외 내부 포트는 실행마다 OS가 임의로 빈 포트를 골라 쓰므로 고정 불요(본진 실측
  근거 재사용).
- `.claude/launch.json`: `netlify` 설정에서 하드코딩 `--port` 플래그 제거, `port` 필드만 8001로.
- `docs/서버_실행_가이드.md` 신설 — 표준 명령·접속 주소표(대시보드는 본진 쪽 하나만 쓰면 된다는
  점 명시)·트러블슈팅. `docs/에이전트_운영.md`에 참조 규칙 추가(순차 실행 규칙은 바로 위 §7
  항목에서 이미 신설돼 있어 추가 조치 없음).
- **적용 파일**: `netlify.toml`, `.claude/launch.json`, `docs/서버_실행_가이드.md`(신규), `docs/
  에이전트_운영.md`(참조 한 줄).
- **검증**: 본진 쪽에서 실기 검증(코드+이름 입장→접두 매칭→매트릭스 표시→재발급 멱등성→QR
  렌더, 양방향 서버 동시 기동)을 이미 마쳤고 동일 코드를 그대로 반영한 것이므로, 이 저장소는
  `node --check` 구문 검사 + 전체 테스트 통과로 동기화 확인을 마무리(지시서가 명시한 대로 "입장
  UI·서버 설정은 양쪽"이되 함수·발급 로직 자체의 실기 검증은 본진에서 수행). `npm test` 148/148
  통과(기존 133 + LMS 운영기초 15건 + 이번 라운드 관련분).
